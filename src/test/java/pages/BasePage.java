package pages;

import utilities.DriverFactory;

public class BasePage {

	//LoginPage loginPage = new LoginPage(DriverFactory.getDriver(),Helper helper);
	//HomePage homePage = new HomePage(DriverFactory.getDriver());
	
}
